# App name: **My To-Do List**
## Description: Display a static list of today's tasks
## Scaffold a Vite project using the React template by entering the following command in the terminal: `npx create-vite@latest --template react .`
## Install dependencies by entering the following command in the terminal: `npm install`
## Run the development server by entering the following command in the terminal: `npm run dev`